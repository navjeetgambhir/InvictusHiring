[pytest]
asyncio_mode = auto
testpaths = backend/tests
pythonpath = backend
markers =
    integration: marks tests as integration tests requiring a live Postgres instance (deselect with -m "not integration")